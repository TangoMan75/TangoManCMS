{% extends 'admin/admin-base.html.twig' %}

{% block title %}{{ parent() }} | Galeries{% endblock %}

{% block admin %}
    <header>
        <h1>Galeries</h1>
        <h2>Liste des galeries</h2>
        <p>Ici vous pouvez accèder à la liste des galeries de <strong>{{ site_name }}</strong>.</p>
    </header>
    {% include "admin/includes/_gallery_tabs.html.twig" %}
    <section id="search">
        <div class="jumbotron">
            <form method="GET" class="form-horizontal">
                <div class="form-group">
                    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
                        <label class="control-label col-sm-4" for="inputTitle">Titre</label>
                        <div class="col-sm-8">
                            <input type="text" name="title" id="inputTitle" class="form-control"
                                   value="{{ app.request.get('title') }}"/>
                        </div>
                    </div>

                    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
                        <label class="control-label col-sm-4" for="inputSlug">Slug</label>
                        <div class="col-sm-8">
                            <input type="text" name="slug" id="inputSlug" class="form-control"
                                   value="{{ app.request.get('slug') }}"/>
                        </div>
                    </div>

                    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
                        <label class="control-label col-sm-4" for="inputPage">Page</label>
                        <div class="col-sm-8">
                            <input type="text" name="a_page" id="inputPage" class="form-control"
                                   value="{{ app.request.get('a_page') }}"/>
                        </div>
                    </div>

                    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
                        <label class="control-label col-sm-4" for="inputMedia">Média</label>
                        <div class="col-sm-8">
                            <input type="text" name="post" id="inputMedia" class="form-control"
                                   value="{{ app.request.get('post') }}"/>
                        </div>
                    </div>

                    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
                        <label class="control-label col-sm-4" for="inputTag">Étiquette</label>
                        <div class="col-sm-8">
                            <input type="text" name="tag" id="inputTag" class="form-control"
                                   value="{{ app.request.get('tag') }}"/>
                        </div>
                    </div>

                    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
                        <label class="control-label col-sm-4" for="selectStatus">Publication</label>
                        <div class="col-sm-8">
                            <select name="published" id="selectStatus" class="form-control">
                                <option value="">Tous</option>
                                <option value="true"
                                        {% if app.request.get('published') == "true" %}selected{% endif %}>
                                    Publié
                                </option>
                                <option value="false"
                                        {% if app.request.get('published') == "false" %}selected{% endif %}>
                                    Non-publié
                                </option>
                            </select>
                        </div>
                    </div>

                    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3 text-right pull-right">
                        <button type="submit" class="btn btn-primary">
                            <span class="glyphicon glyphicon-search"></span> Filtrer ({{ galleries.count }})
                        </button>
                        <button id="reset" class="btn btn-warning">
                            <span class="glyphicon glyphicon-remove"></span> Effacer
                        </button>
                    </div>

                    <input type="hidden" name="gallery" value="1"/>
                    <input type="hidden" name="order" value="{{ app.request.query.get('order', 'title') }}"/>
                    <input type="hidden" name="way" value="{{ app.request.query.get('way', 'ASC') }}"/>
                </div>

            </form>
        </div>
    </section>

    <section id="gallery-list">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                <tr>

                    <th class="{{ app.request.query.get('order') == 'title' ? app.request.query.get('way', 'ASC') }}">
                        <a href="{{ path('app_admin_gallery_index', app.request.query.all|merge({
                            'page'  : 1,
                            'order' : 'title',
                            'way'   : app.request.query.get('order') == 'title'
                            and app.request.query.get('way', 'DESC') == 'DESC' ? 'ASC' : 'DESC'})) }}">
                            Titre
                        </a>
                    </th>

                    <th class="{{ app.request.query.get('order') == 'slug' ? app.request.query.get('way', 'ASC') }}">
                        <a href="{{ path('app_admin_gallery_index', app.request.query.all|merge({
                            'page'  : 1,
                            'order' : 'slug',
                            'way'   : app.request.query.get('order') == 'slug'
                            and app.request.query.get('way', 'ASC') == 'ASC' ? 'DESC' : 'ASC'})) }}">
                            Slug
                        </a>
                    </th>

                    <th class="{{ app.request.query.get('order') == 'j-pages' ? app.request.query.get('way', 'ASC') }}">
                        <a href="{{ path('app_admin_gallery_index', app.request.query.all|merge({
                            'page'  : 1,
                            'order' : 'j-pages',
                            'way'   : app.request.query.get('order') == 'j-pages'
                            and app.request.query.get('way', 'DESC') == 'DESC' ? 'ASC' : 'DESC'})) }}">
                            Pages
                        </a>
                    </th>

                    <th class="{{ app.request.query.get('order') == 'j-posts' ? app.request.query.get('way', 'ASC') }}">
                        <a href="{{ path('app_admin_gallery_index', app.request.query.all|merge({
                            'page'  : 1,
                            'order' : 'j-posts',
                            'way'   : app.request.query.get('order') == 'j-posts'
                            and app.request.query.get('way', 'DESC') == 'DESC' ? 'ASC' : 'DESC'})) }}">
                            Médias
                        </a>
                    </th>

                    <th class="{{ app.request.query.get('order') == 'j-tags' ? app.request.query.get('way', 'ASC') }}">
                        <a href="{{ path('app_admin_gallery_index', app.request.query.all|merge({
                            'page'  : 1,
                            'order' : 'j-tags',
                            'way'   : app.request.query.get('order') == 'j-tags'
                            and app.request.query.get('way', 'DESC') == 'DESC' ? 'ASC' : 'DESC'})) }}">
                            Étiquettes
                        </a>
                    </th>

                    <th class="{{ app.request.query.get('order') == 'created' ? app.request.query.get('way', 'ASC') }}">
                        <a href="{{ path('app_admin_gallery_index', app.request.query.all|merge({
                            'page'  : 1,
                            'order' : 'created',
                            'way'   : app.request.query.get('order') == 'created'
                            and app.request.query.get('way', 'DESC') == 'DESC' ? 'ASC' : 'DESC'})) }}">
                            Création
                        </a>
                    </th>

                    <th class="{{ app.request.query.get('order') == 'modified' ? app.request.query.get('way', 'ASC') }}">
                        <a href="{{ path('app_admin_gallery_index', app.request.query.all|merge({
                            'page'  : 1,
                            'order' : 'modified',
                            'way'   : app.request.query.get('order') == 'modified'
                            and app.request.query.get('way', 'DESC') == 'DESC' ? 'ASC' : 'DESC'})) }}">
                            Modification
                        </a>
                    </th>

                    <th class="{{ app.request.query.get('order') == 'published' ? app.request.query.get('way', 'ASC') }}">
                        <a href="{{ path('app_admin_gallery_index', app.request.query.all|merge({
                            'page'  : 1,
                            'order' : 'published',
                            'way'   : app.request.query.get('order') == 'published'
                            and app.request.query.get('way', 'DESC') == 'DESC' ? 'ASC' : 'DESC'})) }}">
                            Publication
                        </a>
                    </th>

                    <th colspan="2">Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for gallery in galleries %}
                    {% set gallery = gallery[0] %}
                    <tr>
                        <td>
                            <a href="{{ path('app_section_show', { 'slug': gallery.slug }) }}">
                                {{ gallery }}
                            </a>
                        </td>

                        <td>
                            {{ gallery.slug }}
                        </td>

                        <td>
                            {% if gallery.pages|length > 1 %}
                                <a href="{{ path('app_admin_page_index', { 'gallery': gallery.title }) }}"
                                   class="label label-success"
                                   data-toggle="tooltip" data-placement="top"
                                   data-original-title="Lister les {{ gallery.pages|length }} pages contenant la galerie &quot;{{ gallery }}&quot;"
                                >
                                    Pages
                                    <span class="badge-sm text-success">{{ gallery.pages|length }}</span>
                                </a><br/>
                            {% endif %}

                            {% for page in gallery.pages %}
                                {% if loop.index < 6 %}
                                    <a href="{{ path('app_admin_page_index', { 'title': page.title }) }}"
                                       class="label label-primary"
                                       data-toggle="tooltip" data-placement="top"
                                       data-original-title="Voir le détail de la page &quot;{{ page|truncate(20) }}&quot;"
                                    >
                                        {{ page|truncate(20) }}
                                    </a><br/>
                                {% elseif loop.last %}
                                    <span class="label label-primary">...</span>
                                {% endif %}
                            {% else %}
                                <span class="label label-danger">Aucune page</span>
                            {% endfor %}
                        </td>

                        <td>
                            {% if gallery.posts|length > 1 %}
                                <a href="{{ path('app_admin_post_index', { 'gallery': gallery.title }) }}"
                                   class="label label-success"
                                   data-toggle="tooltip" data-placement="top"
                                   data-original-title="Lister les {{ gallery.posts|length }} médias de la galerie &quot;{{ gallery }}&quot;"
                                >
                                    Médias
                                    <span class="badge-sm text-success">{{ gallery.posts|length }}</span>
                                </a><br/>
                            {% endif %}

                            {% for post in gallery.posts %}
                                {% if loop.index < 6 %}
                                    <a href="{{ path('app_admin_post_index', { 'title': post.title }) }}"
                                       class="label label-primary"
                                       data-toggle="tooltip" data-placement="top"
                                       data-original-title="Voir le détail du média &quot;{{ post|truncate(20) }}&quot;"
                                    >
                                        {{ post|truncate(20) }}
                                    </a><br/>
                                {% elseif loop.last %}
                                    <span class="label label-primary">...</span>
                                {% endif %}
                            {% else %}
                                <span class="label label-danger">Aucun média</span>
                            {% endfor %}
                        </td>

                        <td>
                            {% for tag in gallery.tags %}
                                <a href="{{ path('app_admin_gallery_index', app.request.query.all|merge({'tag' : tag.name})) }}"
                                   class="label label-{{ tag.type }}">
                                    {{ tag.name }}
                                </a><br/>
                            {% endfor %}
                        </td>

                        <td>
                            <span class="small">
                                {{ gallery.created|date('d/m/Y H:i:s') }}
                            </span>
                        </td>

                        <td>
                            <span class="small">
                                {{ gallery.modified|date('d/m/Y H:i:s') }}
                            </span>
                        </td>

                        <td>
                            {% if gallery.published %}
                                <a href="{{ path('app_admin_gallery_publish', { 'id': gallery.id, 'publish': 0, 'callback': app.request.uri|callback }) }}"
                                   class="btn btn-success btn-xs" data-toggle="tooltip" data-placement="top"
                                   data-original-title="Retirer la publication">
                                    <span class="glyphicon glyphicon-ok"></span>
                                </a>
                            {% else %}
                                <a href="{{ path('app_admin_gallery_publish', { 'id': gallery.id, 'publish': 1, 'callback': app.request.uri|callback }) }}"
                                   class="btn btn-warning btn-xs" data-toggle="tooltip" data-placement="top"
                                   data-original-title="Publier cette galerie">
                                    <span class="glyphicon glyphicon-remove"></span>
                                </a>
                            {% endif %}
                        </td>

                        <td>
                            <a href="{{ path('app_admin_gallery_edit', { 'id': gallery.id, 'callback': app.request.uri|callback }) }}"
                               class="btn btn-warning btn-sm"
                               data-toggle="tooltip" data-placement="top"
                               data-original-title="Modifier la galerie &quot;{{ gallery }}&quot;"
                            >
                                <span class="glyphicon glyphicon-edit"></span>
                                <span class="hidden-xs">Modifier</span>
                            </a>
                        </td>

                        <td data-toggle="tooltip" data-placement="top"
                            data-original-title="Supprimer la galerie &quot;{{ gallery }}&quot;"
                        >
                            <button type="button" class="btn btn-danger btn-sm" data-toggle="modal"
                                    data-target=".my-modal" data-item="{{ gallery }}"
                                    data-path="{{ path('app_admin_gallery_delete', { 'id': gallery.id, 'callback': app.request.uri|remove_query }) }}">
                                <span class="glyphicon glyphicon-trash"></span>
                                <span class="hidden-xs">Supprimer</span>
                            </button>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <div class="text-center">
            {{ pagination(galleries) }}
        </div>
    </section>
    {% include "admin/includes/_delete-gallery-modal.html.twig" %}
{% endblock %}

{% block javascript %}
    {{ parent() }}
    <script src="{{ asset('js/modal.js') }}"></script>
    <script>
        $('#reset').click(function () {
            $('form').find('input:text, input:password, select, textarea').val('');
            $('form').find('input:radio, input:checkbox').prop('checked', false);
        });
    </script>
{% endblock %}