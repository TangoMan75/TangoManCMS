{% extends 'admin/admin-base.html.twig' %}

{% block title %}{{ parent() }} | Étiquettes - Modifier {{ tag.name }}{% endblock %}

{% block head %}
    {{ parent() }}
    {% if app.user %}
        <script src="//cdn.tinymce.com/4/tinymce.min.js"></script>
        <script>tinymce.init({selector: 'textarea', height: '250'});</script>
    {% endif %}
{% endblock %}

{% block admin %}
    <header>
        <h1>Étiquettes</h1>
        <h2>Modifier l'étiquette &quot;{{ tag.name }}&quot;</h2>
        <p>Ici vous pouvez modifier l'étiquettes &quot;{{ tag.name }}&quot; de {{ site_name }}</p>
    </header>
    {% include "admin/includes/_tag_tabs.html.twig" %}
    <section id="edit">
        <div class="jumbotron">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary center-block">
                <span class="glyphicon glyphicon-send"></span>
                Valider
            </button>
            {{ form_end(form) }}
        </div>
    </section>
{% endblock %}
