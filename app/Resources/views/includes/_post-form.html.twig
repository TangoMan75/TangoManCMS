<div id="post-form" class="jumbotron">
    <h2 class="text-center"><span class="glyphicon glyphicon-pencil pull-left"></span>Publier un article</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary center-block"><span class="glyphicon glyphicon-send"></span>Publier
    </button>
    {{ form_end(form) }}
</div>
