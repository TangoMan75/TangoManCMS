{% extends "::email-base.html.twig" %}
{% block body %}
    <h1>Bonjour {{ user.username|capitalize }}</h1>
    <p>
        Une demande de {{ email.title|lower }} sur
        <a href="{{ absolute_url(path('homepage')) }}">
            {{ domain_name }}
        </a>
        a été formulée.
    </p>
    <p>Pour {{ email.description }} cliquez sur le bouton suivant :<br />
        <a class="btn" href="{{ absolute_url(path('app_token_token', {'token': email.token })) }}">
            {{ email.btn }}
        </a>
    </p>
    <p>Ce lien n'est valable que pour une durée de 24 heures.</p>
    {% if token.reset is defined %}
        <p>Si vous n'êtes pas à l'origine de cette demande il est probable que votre compte ait été compromi,
        nous vous recommandons fortement de <a href="{{ absolute_url(path('app_token_token', {'token': email.reset })) }}">renouveller votre mot de passe</a>.</p>
    {% else %}
        <p>Si vous n'êtes pas à l'origine de cette demande, vous pouvez ignorer ce message. 
        Votre compte n'a pas été compromis, vous pourrez toujours vous connecter avec vos identifiants habituels.</p>
    {% endif %}
    <p>Cordialement</p>
{% endblock %}